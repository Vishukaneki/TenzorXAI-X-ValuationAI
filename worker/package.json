{
  "name": "collateral-narrative-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --remote --port 8787",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.53.0"
  }
}
